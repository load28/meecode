# MeeCode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code CLI를 PTY로 실행하는 Tauri 2 데스크탑 앱. 응답이 500자를 초과하면 우측 패널에 마크다운을 렌더링하고, 텍스트 선택 시 해당 구절에 대한 코멘트를 Claude Code로 직접 전송할 수 있다.

**Architecture:** Rust 백엔드가 `portable-pty`로 `claude` CLI를 자식 프로세스로 실행. stdout은 `pty:data` 이벤트(xterm.js용)와 `md:update` 이벤트(마크다운 패널용)로 팬아웃. 코멘트 제출은 `write_input` Tauri 커맨드를 통해 PTY stdin에 직접 주입.

**Tech Stack:** Tauri 2, Rust (`portable-pty`, `dirs`, `strip-ansi-escapes`), React + TypeScript, xterm.js, marked, DOMPurify, react-resizable-panels, Vitest + React Testing Library

---

## File Map

```
meecode/
├── .gitignore
├── package.json
├── index.html
├── vite.config.ts
├── vitest.config.ts
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── App.css
│   ├── types.ts
│   ├── test/
│   │   └── setup.ts                     # Vitest globals + Tauri API mocks
│   ├── hooks/
│   │   ├── usePtyStream.ts              # md:update 이벤트 구독, 마크다운 상태
│   │   ├── usePtyStream.test.ts
│   │   ├── useSelection.ts              # mouseup 셀렉션 감지
│   │   └── useSelection.test.ts
│   └── components/
│       ├── TerminalPane/
│       │   ├── index.tsx                # xterm.js 래퍼, pty:data 구독
│       │   ├── TerminalPane.test.tsx
│       │   └── TerminalPane.css
│       ├── MarkdownPane/
│       │   ├── index.tsx                # marked 렌더링 + useSelection
│       │   ├── MarkdownPane.test.tsx
│       │   └── MarkdownPane.css
│       └── CommentFloat/
│           ├── index.tsx                # 플로팅 버튼 → 플로팅 인풋
│           ├── CommentFloat.test.tsx
│           └── CommentFloat.css
└── src-tauri/
    ├── Cargo.toml
    ├── tauri.conf.json
    └── src/
        ├── main.rs                      # run() 호출
        ├── lib.rs                       # Tauri Builder, AppState
        ├── config.rs                    # Config 구조체, load/save
        ├── pty_manager.rs               # PTY 생성, stdout 팬아웃
        └── commands.rs                  # Tauri IPC 커맨드
```

---

## Task 1: Project Scaffold

**Files:**
- Create: `package.json`, `vite.config.ts`, `index.html`, `.gitignore`, `src/main.tsx`, `src/App.tsx`, `src-tauri/Cargo.toml`

- [ ] **Step 1: Tauri 2 + React + TypeScript 프로젝트 생성**

```bash
cd /Users/minyeoung/Downloads/source/meecode
npm create tauri-app@latest . -- --template react-ts --manager npm --force
```

프롬프트 응답:
- Project name: `meecode`
- Choose your frontend framework: `React`
- Choose your package manager: `npm`

- [ ] **Step 2: 프론트엔드 의존성 설치**

```bash
npm install @xterm/xterm @xterm/addon-fit marked dompurify highlight.js react-resizable-panels
npm install -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom @vitest/coverage-v8 @types/dompurify
```

- [ ] **Step 3: Rust 의존성 추가 (`src-tauri/Cargo.toml`)**

기존 `[dependencies]` 섹션에 아래를 추가:

```toml
portable-pty = "0.8"
dirs = "5"
strip-ansi-escapes = "0.2"

[dev-dependencies]
tempfile = "3"
```

- [ ] **Step 4: `.gitignore` 업데이트**

```
node_modules/
dist/
src-tauri/target/
.superpowers/
```

- [ ] **Step 5: `vite.config.ts` 설정 — Tauri 전용 포트 고정**

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  clearScreen: false,
  server: {
    port: 1420,
    strictPort: true,
    watch: { ignored: ['**/src-tauri/**'] },
  },
})
```

- [ ] **Step 6: `vitest.config.ts` 생성**

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

- [ ] **Step 7: `src/types.ts` 생성 — 공유 타입 정의**

```typescript
export interface Config {
  markdown_threshold: number;
  claude_path: string | null;
}

export interface SelectionState {
  text: string;
  rect: DOMRect | null;
}
```

- [ ] **Step 8: `package.json`에 test 스크립트 추가**

`"scripts"` 섹션에:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 9: 빌드 확인**

```bash
npm run build
```

Expected: 오류 없이 `dist/` 생성

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat: scaffold Tauri 2 + React + TypeScript project"
```

---

## Task 2: Rust Config Module

**Files:**
- Create: `src-tauri/src/config.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: `config.rs` 생성 — 실패하는 테스트 먼저 작성**

```rust
// src-tauri/src/config.rs
use serde::{Deserialize, Serialize};
use std::fs;
use std::path::PathBuf;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Config {
    pub markdown_threshold: usize,
    pub claude_path: Option<String>,
}

impl Default for Config {
    fn default() -> Self {
        Self {
            markdown_threshold: 500,
            claude_path: None,
        }
    }
}

impl Config {
    pub fn config_path() -> PathBuf {
        dirs::config_dir()
            .unwrap_or_else(|| PathBuf::from("."))
            .join("meecode")
            .join("config.json")
    }

    pub fn load() -> Self {
        Self::load_from(&Self::config_path())
    }

    pub fn load_from(path: &PathBuf) -> Self {
        if !path.exists() {
            return Self::default();
        }
        let content = fs::read_to_string(path).unwrap_or_default();
        serde_json::from_str(&content).unwrap_or_default()
    }

    pub fn save(&self) -> Result<(), String> {
        self.save_to(&Self::config_path())
    }

    pub fn save_to(&self, path: &PathBuf) -> Result<(), String> {
        if let Some(parent) = path.parent() {
            fs::create_dir_all(parent).map_err(|e| e.to_string())?;
        }
        let content = serde_json::to_string_pretty(self).map_err(|e| e.to_string())?;
        fs::write(path, content).map_err(|e| e.to_string())
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::tempdir;

    #[test]
    fn test_default_threshold() {
        let config = Config::default();
        assert_eq!(config.markdown_threshold, 500);
        assert!(config.claude_path.is_none());
    }

    #[test]
    fn test_save_and_load_roundtrip() {
        let dir = tempdir().unwrap();
        let path = dir.path().join("config.json");

        let config = Config {
            markdown_threshold: 1000,
            claude_path: Some("custom_claude".into()),
        };
        config.save_to(&path).unwrap();

        let loaded = Config::load_from(&path);
        assert_eq!(loaded.markdown_threshold, 1000);
        assert_eq!(loaded.claude_path, Some("custom_claude".into()));
    }

    #[test]
    fn test_load_from_nonexistent_returns_default() {
        let path = PathBuf::from("/tmp/meecode_nonexistent_xyz/config.json");
        let config = Config::load_from(&path);
        assert_eq!(config.markdown_threshold, 500);
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd src-tauri && cargo test config
```

Expected: `error[E0425]: cannot find function` 또는 컴파일 오류 (모듈 등록 전)

- [ ] **Step 3: `lib.rs`에 config 모듈 등록**

```rust
// src-tauri/src/lib.rs 상단에 추가
mod config;
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cargo test config
```

Expected:
```
test config::tests::test_default_threshold ... ok
test config::tests::test_load_from_nonexistent_returns_default ... ok
test config::tests::test_save_and_load_roundtrip ... ok
```

- [ ] **Step 5: Commit**

```bash
cd ..
git add src-tauri/src/config.rs src-tauri/src/lib.rs
git commit -m "feat(rust): add Config module with load/save and tests"
```

---

## Task 3: Rust PTY Manager

**Files:**
- Create: `src-tauri/src/pty_manager.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: ANSI 스트리핑 유틸 테스트 작성**

`src-tauri/src/pty_manager.rs` 파일 생성 후 테스트부터:

```rust
// src-tauri/src/pty_manager.rs
use portable_pty::{native_pty_system, CommandBuilder, PtySize};
use std::io::{Read, Write};
use std::sync::{Arc, Mutex};
use tauri::{AppHandle, Emitter};

pub fn strip_ansi(input: &[u8]) -> String {
    let stripped = strip_ansi_escapes::strip(input);
    String::from_utf8_lossy(&stripped)
        .replace('\r', "")
        .to_string()
}

pub struct PtyManager {
    writer: Arc<Mutex<Box<dyn Write + Send>>>,
    md_buffer: Arc<Mutex<String>>,
}

impl PtyManager {
    pub fn spawn(app: AppHandle, claude_cmd: &str, threshold: usize) -> Result<Self, String> {
        let pty_system = native_pty_system();
        let pty = pty_system
            .openpty(PtySize {
                rows: 24,
                cols: 220,
                pixel_width: 0,
                pixel_height: 0,
            })
            .map_err(|e| e.to_string())?;

        let cmd = CommandBuilder::new(claude_cmd);
        pty.slave
            .spawn_command(cmd)
            .map_err(|e| e.to_string())?;

        let writer = Arc::new(Mutex::new(
            pty.master.take_writer().map_err(|e| e.to_string())?,
        ));
        let mut reader = pty.master.try_clone_reader().map_err(|e| e.to_string())?;
        let md_buffer: Arc<Mutex<String>> = Arc::new(Mutex::new(String::new()));
        let md_buffer_reader = md_buffer.clone();

        std::thread::spawn(move || {
            let mut buf = [0u8; 4096];
            loop {
                match reader.read(&mut buf) {
                    Ok(0) | Err(_) => break,
                    Ok(n) => {
                        let raw = &buf[..n];
                        // raw 데이터를 xterm.js로 전송 (ANSI 코드 포함)
                        let raw_str = String::from_utf8_lossy(raw).to_string();
                        app.emit("pty:data", &raw_str).ok();

                        // ANSI 제거 후 마크다운 버퍼에 누적
                        let clean = strip_ansi(raw);
                        let mut buf_lock = md_buffer_reader.lock().unwrap();
                        buf_lock.push_str(&clean);

                        if buf_lock.len() >= threshold {
                            app.emit("md:update", buf_lock.clone()).ok();
                        }
                    }
                }
            }
        });

        Ok(PtyManager { writer, md_buffer })
    }

    pub fn write_input(&self, text: &str) -> Result<(), String> {
        // 새 입력 = 새 응답 시작 → 버퍼 리셋
        *self.md_buffer.lock().map_err(|e| e.to_string())? = String::new();
        let mut w = self.writer.lock().map_err(|e| e.to_string())?;
        w.write_all(text.as_bytes()).map_err(|e| e.to_string())
    }

    pub fn resize(&self, rows: u16, cols: u16) -> Result<(), String> {
        // TODO: PTY resize — portable-pty master에 ioctl 필요
        // 현재는 고정 크기 (220x24) 사용
        let _ = (rows, cols);
        Ok(())
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_strip_ansi_color_codes() {
        let input = b"\x1b[32mHello\x1b[0m";
        assert_eq!(strip_ansi(input), "Hello");
    }

    #[test]
    fn test_strip_ansi_plain_text() {
        assert_eq!(strip_ansi(b"plain text"), "plain text");
    }

    #[test]
    fn test_strip_ansi_complex_sequence() {
        let input = b"\x1b[1;31mError:\x1b[0m message";
        assert_eq!(strip_ansi(input), "Error: message");
    }

    #[test]
    fn test_strip_ansi_removes_carriage_return() {
        assert_eq!(strip_ansi(b"line1\r\nline2"), "line1\nline2");
    }
}
```

- [ ] **Step 2: 테스트 실패 확인 (모듈 미등록 상태)**

```bash
cd src-tauri && cargo test pty_manager
```

Expected: 컴파일 오류 (모듈 미등록)

- [ ] **Step 3: `lib.rs`에 pty_manager 모듈 등록**

```rust
// src-tauri/src/lib.rs 상단에 추가
mod pty_manager;
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cargo test pty_manager
```

Expected:
```
test pty_manager::tests::test_strip_ansi_color_codes ... ok
test pty_manager::tests::test_strip_ansi_plain_text ... ok
test pty_manager::tests::test_strip_ansi_complex_sequence ... ok
test pty_manager::tests::test_strip_ansi_removes_carriage_return ... ok
```

- [ ] **Step 5: Commit**

```bash
cd ..
git add src-tauri/src/pty_manager.rs src-tauri/src/lib.rs
git commit -m "feat(rust): add PtyManager with PTY spawn and ANSI stripping"
```

---

## Task 4: Tauri Commands + App State

**Files:**
- Create: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/lib.rs`, `src-tauri/src/main.rs`

- [ ] **Step 1: `commands.rs` 생성**

```rust
// src-tauri/src/commands.rs
use crate::config::Config;
use crate::pty_manager::PtyManager;
use std::sync::Mutex;
use tauri::State;

pub struct AppState {
    pub pty: Mutex<Option<PtyManager>>,
    pub config: Mutex<Config>,
}

impl AppState {
    pub fn new() -> Self {
        Self {
            pty: Mutex::new(None),
            config: Mutex::new(Config::load()),
        }
    }
}

#[tauri::command]
pub fn start_session(
    app: tauri::AppHandle,
    state: State<AppState>,
) -> Result<(), String> {
    let config = state.config.lock().map_err(|e| e.to_string())?.clone();
    let claude_cmd = config.claude_path.as_deref().unwrap_or("claude");
    let threshold = config.markdown_threshold;

    let manager = PtyManager::spawn(app, claude_cmd, threshold)?;
    *state.pty.lock().map_err(|e| e.to_string())? = Some(manager);
    Ok(())
}

#[tauri::command]
pub fn write_input(text: String, state: State<AppState>) -> Result<(), String> {
    let pty = state.pty.lock().map_err(|e| e.to_string())?;
    match pty.as_ref() {
        Some(manager) => manager.write_input(&text),
        None => Err("PTY session not started".into()),
    }
}

#[tauri::command]
pub fn get_config(state: State<AppState>) -> Result<Config, String> {
    Ok(state.config.lock().map_err(|e| e.to_string())?.clone())
}

#[tauri::command]
pub fn set_config(config: Config, state: State<AppState>) -> Result<(), String> {
    config.save()?;
    *state.config.lock().map_err(|e| e.to_string())? = config;
    Ok(())
}
```

- [ ] **Step 2: `lib.rs`를 Tauri Builder로 완성**

```rust
// src-tauri/src/lib.rs
mod commands;
mod config;
mod pty_manager;

use commands::AppState;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .manage(AppState::new())
        .invoke_handler(tauri::generate_handler![
            commands::start_session,
            commands::write_input,
            commands::get_config,
            commands::set_config,
        ])
        .run(tauri::generate_context!())
        .expect("error running meecode");
}
```

- [ ] **Step 3: `main.rs` 확인 (scaffold가 이미 생성했으면 스킵)**

```rust
// src-tauri/src/main.rs
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    meecode_lib::run();
}
```

- [ ] **Step 4: Rust 컴파일 확인**

```bash
cd src-tauri && cargo build 2>&1 | tail -5
```

Expected: `Finished dev [unoptimized + debuginfo] target(s)`

- [ ] **Step 5: Commit**

```bash
cd ..
git add src-tauri/src/commands.rs src-tauri/src/lib.rs src-tauri/src/main.rs
git commit -m "feat(rust): add Tauri commands (start_session, write_input, get_config, set_config)"
```

---

## Task 5: Frontend Test Infrastructure

**Files:**
- Create: `src/test/setup.ts`

- [ ] **Step 1: Vitest setup 파일 생성 — Tauri API 모킹**

```typescript
// src/test/setup.ts
import '@testing-library/jest-dom'
import { vi } from 'vitest'

// @tauri-apps/api/event 모킹
vi.mock('@tauri-apps/api/event', () => ({
  listen: vi.fn(() => Promise.resolve(() => {})),
}))

// @tauri-apps/api/core 모킹
vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn(() => Promise.resolve()),
}))
```

- [ ] **Step 2: 모킹 동작 확인 — 더미 테스트 실행**

```bash
npm test
```

Expected: `No test files found` 또는 0개 테스트 패스 (아직 테스트 없음)

- [ ] **Step 3: Commit**

```bash
git add src/test/setup.ts vitest.config.ts
git commit -m "test: add Vitest setup with Tauri API mocks"
```

---

## Task 6: useSelection Hook

**Files:**
- Create: `src/hooks/useSelection.ts`
- Create: `src/hooks/useSelection.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/hooks/useSelection.test.ts
import { renderHook, act } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { useSelection } from './useSelection'

describe('useSelection', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  it('초기 상태에서 text가 빈 문자열, rect가 null', () => {
    vi.spyOn(window, 'getSelection').mockReturnValue(null)
    const { result } = renderHook(() => useSelection())
    expect(result.current.selection.text).toBe('')
    expect(result.current.selection.rect).toBeNull()
  })

  it('텍스트 선택 시 text와 rect를 캡처', () => {
    const mockRect = {
      top: 100, left: 50, width: 100, height: 20,
      bottom: 120, right: 150, x: 50, y: 100,
      toJSON: () => ({}),
    } as DOMRect

    vi.spyOn(window, 'getSelection').mockReturnValue({
      isCollapsed: false,
      toString: () => 'await를 사용',
      getRangeAt: () => ({ getBoundingClientRect: () => mockRect }),
    } as unknown as Selection)

    const { result } = renderHook(() => useSelection())
    act(() => { result.current.handleMouseUp() })

    expect(result.current.selection.text).toBe('await를 사용')
    expect(result.current.selection.rect).toBe(mockRect)
  })

  it('빈 선택(isCollapsed)이면 selection 초기화', () => {
    vi.spyOn(window, 'getSelection').mockReturnValue({
      isCollapsed: true,
    } as unknown as Selection)

    const { result } = renderHook(() => useSelection())
    act(() => { result.current.handleMouseUp() })

    expect(result.current.selection.text).toBe('')
    expect(result.current.selection.rect).toBeNull()
  })

  it('clearSelection 호출 시 상태 초기화', () => {
    const mockRect = {
      top: 100, left: 50, width: 100, height: 20,
      bottom: 120, right: 150, x: 50, y: 100,
      toJSON: () => ({}),
    } as DOMRect

    vi.spyOn(window, 'getSelection').mockReturnValue({
      isCollapsed: false,
      toString: () => 'some text',
      getRangeAt: () => ({ getBoundingClientRect: () => mockRect }),
    } as unknown as Selection)

    const { result } = renderHook(() => useSelection())
    act(() => { result.current.handleMouseUp() })
    expect(result.current.selection.text).toBe('some text')

    act(() => { result.current.clearSelection() })
    expect(result.current.selection.text).toBe('')
    expect(result.current.selection.rect).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test src/hooks/useSelection.test.ts
```

Expected: `Cannot find module './useSelection'`

- [ ] **Step 3: `useSelection.ts` 구현**

```typescript
// src/hooks/useSelection.ts
import { useState, useCallback } from 'react'
import type { SelectionState } from '../types'

interface UseSelectionReturn {
  selection: SelectionState
  handleMouseUp: () => void
  clearSelection: () => void
}

export function useSelection(): UseSelectionReturn {
  const [selection, setSelection] = useState<SelectionState>({ text: '', rect: null })

  const handleMouseUp = useCallback(() => {
    const sel = window.getSelection()
    if (!sel || sel.isCollapsed) {
      setSelection({ text: '', rect: null })
      return
    }
    const text = sel.toString().trim()
    if (!text) {
      setSelection({ text: '', rect: null })
      return
    }
    const rect = sel.getRangeAt(0).getBoundingClientRect()
    setSelection({ text, rect })
  }, [])

  const clearSelection = useCallback(() => {
    setSelection({ text: '', rect: null })
  }, [])

  return { selection, handleMouseUp, clearSelection }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test src/hooks/useSelection.test.ts
```

Expected:
```
✓ 초기 상태에서 text가 빈 문자열, rect가 null
✓ 텍스트 선택 시 text와 rect를 캡처
✓ 빈 선택(isCollapsed)이면 selection 초기화
✓ clearSelection 호출 시 상태 초기화
```

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useSelection.ts src/hooks/useSelection.test.ts
git commit -m "feat(frontend): add useSelection hook with tests"
```

---

## Task 7: usePtyStream Hook

**Files:**
- Create: `src/hooks/usePtyStream.ts`
- Create: `src/hooks/usePtyStream.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/hooks/usePtyStream.test.ts
import { renderHook, act, waitFor } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { listen } from '@tauri-apps/api/event'
import { usePtyStream } from './usePtyStream'

describe('usePtyStream', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('초기 상태: markdownContent 빈 문자열, isMarkdownVisible false', () => {
    const { result } = renderHook(() => usePtyStream())
    expect(result.current.markdownContent).toBe('')
    expect(result.current.isMarkdownVisible).toBe(false)
  })

  it('md:update 이벤트 수신 시 마크다운 상태 업데이트', async () => {
    let mdHandler: ((e: { payload: string }) => void) | null = null

    vi.mocked(listen).mockImplementation(async (event, handler) => {
      if (event === 'md:update') {
        mdHandler = handler as (e: { payload: string }) => void
      }
      return () => {}
    })

    const { result } = renderHook(() => usePtyStream())

    await act(async () => {
      mdHandler?.({ payload: '# 제목\n\n본문 내용' })
    })

    await waitFor(() => {
      expect(result.current.isMarkdownVisible).toBe(true)
      expect(result.current.markdownContent).toBe('# 제목\n\n본문 내용')
    })
  })

  it('md:update 이벤트마다 content 업데이트', async () => {
    let mdHandler: ((e: { payload: string }) => void) | null = null

    vi.mocked(listen).mockImplementation(async (event, handler) => {
      if (event === 'md:update') {
        mdHandler = handler as (e: { payload: string }) => void
      }
      return () => {}
    })

    const { result } = renderHook(() => usePtyStream())

    await act(async () => {
      mdHandler?.({ payload: '# 첫 번째 응답' })
    })
    await act(async () => {
      mdHandler?.({ payload: '# 두 번째 응답 (더 긴 내용)' })
    })

    await waitFor(() => {
      expect(result.current.markdownContent).toBe('# 두 번째 응답 (더 긴 내용)')
    })
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test src/hooks/usePtyStream.test.ts
```

Expected: `Cannot find module './usePtyStream'`

- [ ] **Step 3: `usePtyStream.ts` 구현**

```typescript
// src/hooks/usePtyStream.ts
import { useEffect, useState } from 'react'
import { listen } from '@tauri-apps/api/event'

interface PtyStreamState {
  markdownContent: string
  isMarkdownVisible: boolean
}

export function usePtyStream(): PtyStreamState {
  const [state, setState] = useState<PtyStreamState>({
    markdownContent: '',
    isMarkdownVisible: false,
  })

  useEffect(() => {
    const unlistenMd = listen<string>('md:update', (event) => {
      setState({
        markdownContent: event.payload,
        isMarkdownVisible: true,
      })
    })

    return () => {
      unlistenMd.then((fn) => fn())
    }
  }, [])

  return state
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test src/hooks/usePtyStream.test.ts
```

Expected:
```
✓ 초기 상태: markdownContent 빈 문자열, isMarkdownVisible false
✓ md:update 이벤트 수신 시 마크다운 상태 업데이트
✓ md:update 이벤트마다 content 업데이트
```

- [ ] **Step 5: Commit**

```bash
git add src/hooks/usePtyStream.ts src/hooks/usePtyStream.test.ts
git commit -m "feat(frontend): add usePtyStream hook with tests"
```

---

## Task 8: TerminalPane Component

**Files:**
- Create: `src/components/TerminalPane/index.tsx`
- Create: `src/components/TerminalPane/TerminalPane.css`
- Create: `src/components/TerminalPane/TerminalPane.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/components/TerminalPane/TerminalPane.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { invoke } from '@tauri-apps/api/core'
import { TerminalPane } from './index'

// xterm.js는 실제 DOM 렌더링이 필요하므로 모킹
vi.mock('@xterm/xterm', () => ({
  Terminal: vi.fn().mockImplementation(() => ({
    open: vi.fn(),
    write: vi.fn(),
    onData: vi.fn(),
    dispose: vi.fn(),
    loadAddon: vi.fn(),
  })),
}))
vi.mock('@xterm/addon-fit', () => ({
  FitAddon: vi.fn().mockImplementation(() => ({
    fit: vi.fn(),
    activate: vi.fn(),
  })),
}))

describe('TerminalPane', () => {
  it('터미널 컨테이너 div를 렌더링', () => {
    const { container } = render(<TerminalPane />)
    expect(container.querySelector('.terminal-container')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test src/components/TerminalPane/TerminalPane.test.tsx
```

Expected: `Cannot find module './index'`

- [ ] **Step 3: `TerminalPane/index.tsx` 구현**

```typescript
// src/components/TerminalPane/index.tsx
import { useEffect, useRef } from 'react'
import { Terminal } from '@xterm/xterm'
import { FitAddon } from '@xterm/addon-fit'
import { listen } from '@tauri-apps/api/event'
import { invoke } from '@tauri-apps/api/core'
import '@xterm/xterm/css/xterm.css'
import './TerminalPane.css'

export function TerminalPane() {
  const containerRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (!containerRef.current) return

    const term = new Terminal({
      cursorBlink: true,
      fontSize: 14,
      fontFamily: 'Menlo, Monaco, "Courier New", monospace',
      theme: {
        background: '#0d1117',
        foreground: '#c9d1d9',
        cursor: '#58a6ff',
      },
    })

    const fitAddon = new FitAddon()
    term.loadAddon(fitAddon)
    term.open(containerRef.current)
    fitAddon.fit()

    term.onData((data) => {
      invoke('write_input', { text: data }).catch(console.error)
    })

    const unlistenPty = listen<string>('pty:data', (event) => {
      term.write(event.payload)
    })

    const resizeObserver = new ResizeObserver(() => fitAddon.fit())
    resizeObserver.observe(containerRef.current)

    return () => {
      resizeObserver.disconnect()
      unlistenPty.then((fn) => fn())
      term.dispose()
    }
  }, [])

  return <div ref={containerRef} className="terminal-container" />
}
```

- [ ] **Step 4: `TerminalPane.css` 생성**

```css
/* src/components/TerminalPane/TerminalPane.css */
.terminal-container {
  width: 100%;
  height: 100%;
  background: #0d1117;
  overflow: hidden;
}
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npm test src/components/TerminalPane/TerminalPane.test.tsx
```

Expected: `✓ 터미널 컨테이너 div를 렌더링`

- [ ] **Step 6: Commit**

```bash
git add src/components/TerminalPane/
git commit -m "feat(frontend): add TerminalPane component with xterm.js"
```

---

## Task 9: CommentFloat Component

**Files:**
- Create: `src/components/CommentFloat/index.tsx`
- Create: `src/components/CommentFloat/CommentFloat.css`
- Create: `src/components/CommentFloat/CommentFloat.test.tsx`

CommentFloat부터 먼저 구현 (MarkdownPane이 이것을 사용)

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/components/CommentFloat/CommentFloat.test.tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { invoke } from '@tauri-apps/api/core'
import { CommentFloat } from './index'

const mockRect = {
  top: 200, left: 100, width: 80, height: 20,
  bottom: 220, right: 180, x: 100, y: 200,
  toJSON: () => ({}),
} as DOMRect

const mockSelection = { text: 'await를 사용', rect: mockRect }

describe('CommentFloat', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('💬 코멘트 버튼을 초기에 렌더링', () => {
    render(<CommentFloat selection={mockSelection} onClose={() => {}} />)
    expect(screen.getByText('💬 코멘트')).toBeInTheDocument()
  })

  it('버튼 클릭 시 버튼 사라지고 인풋창 표시', async () => {
    const user = userEvent.setup()
    render(<CommentFloat selection={mockSelection} onClose={() => {}} />)

    await user.click(screen.getByText('💬 코멘트'))

    expect(screen.queryByText('💬 코멘트')).not.toBeInTheDocument()
    expect(screen.getByPlaceholderText('질문을 입력하세요...')).toBeInTheDocument()
  })

  it('엔터 제출 시 [선택: "..."] 포맷으로 write_input 호출', async () => {
    const user = userEvent.setup()
    const onClose = vi.fn()
    render(<CommentFloat selection={mockSelection} onClose={onClose} />)

    await user.click(screen.getByText('💬 코멘트'))
    await user.type(screen.getByPlaceholderText('질문을 입력하세요...'), 'async 없으면?')
    await user.keyboard('{Enter}')

    expect(invoke).toHaveBeenCalledWith('write_input', {
      text: '[선택: "await를 사용"] async 없으면?\n',
    })
    expect(onClose).toHaveBeenCalledOnce()
  })

  it('전송 버튼 클릭으로도 제출 가능', async () => {
    const user = userEvent.setup()
    const onClose = vi.fn()
    render(<CommentFloat selection={mockSelection} onClose={onClose} />)

    await user.click(screen.getByText('💬 코멘트'))
    await user.type(screen.getByPlaceholderText('질문을 입력하세요...'), '질문 내용')
    await user.click(screen.getByText('전송'))

    expect(invoke).toHaveBeenCalledWith('write_input', {
      text: '[선택: "await를 사용"] 질문 내용\n',
    })
  })

  it('ESC 키로 닫기', async () => {
    const user = userEvent.setup()
    const onClose = vi.fn()
    render(<CommentFloat selection={mockSelection} onClose={onClose} />)

    await user.keyboard('{Escape}')
    expect(onClose).toHaveBeenCalledOnce()
  })

  it('빈 입력은 제출하지 않음', async () => {
    const user = userEvent.setup()
    render(<CommentFloat selection={mockSelection} onClose={() => {}} />)

    await user.click(screen.getByText('💬 코멘트'))
    await user.keyboard('{Enter}')

    expect(invoke).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test src/components/CommentFloat/CommentFloat.test.tsx
```

Expected: `Cannot find module './index'`

- [ ] **Step 3: `CommentFloat/index.tsx` 구현**

```typescript
// src/components/CommentFloat/index.tsx
import { useState, useEffect } from 'react'
import { invoke } from '@tauri-apps/api/core'
import type { SelectionState } from '../../types'
import './CommentFloat.css'

interface Props {
  selection: SelectionState & { rect: DOMRect }
  onClose: () => void
}

export function CommentFloat({ selection, onClose }: Props) {
  const [showInput, setShowInput] = useState(false)
  const [input, setInput] = useState('')

  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose()
    }
    document.addEventListener('keydown', handleKeyDown)
    return () => document.removeEventListener('keydown', handleKeyDown)
  }, [onClose])

  const handleSubmit = async () => {
    if (!input.trim()) return
    const message = `[선택: "${selection.text}"] ${input.trim()}\n`
    await invoke('write_input', { text: message })
    setInput('')
    onClose()
  }

  const style: React.CSSProperties = {
    position: 'fixed',
    top: selection.rect.top - 44,
    left: selection.rect.left,
    zIndex: 1000,
  }

  return (
    <div style={style} className="comment-float">
      {!showInput ? (
        <button
          className="comment-float__button"
          onClick={() => setShowInput(true)}
        >
          💬 코멘트
        </button>
      ) : (
        <div className="comment-float__input-row">
          <input
            autoFocus
            className="comment-float__input"
            placeholder="질문을 입력하세요..."
            value={input}
            onChange={(e) => setInput(e.target.value)}
            onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}
          />
          <button className="comment-float__send" onClick={handleSubmit}>
            전송
          </button>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: `CommentFloat.css` 생성**

```css
/* src/components/CommentFloat/CommentFloat.css */
.comment-float {
  display: flex;
  align-items: center;
  filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.5));
}

.comment-float__button {
  background: #1f6feb;
  color: #fff;
  border: none;
  border-radius: 6px;
  padding: 5px 12px;
  font-size: 13px;
  cursor: pointer;
  white-space: nowrap;
}

.comment-float__button:hover {
  background: #388bfd;
}

.comment-float__input-row {
  display: flex;
  gap: 6px;
  background: #161b22;
  border: 1px solid #1f6feb;
  border-radius: 8px;
  padding: 6px 8px;
}

.comment-float__input {
  width: 280px;
  background: #0d1117;
  border: 1px solid #30363d;
  border-radius: 5px;
  padding: 5px 10px;
  color: #c9d1d9;
  font-size: 13px;
  outline: none;
}

.comment-float__input:focus {
  border-color: #58a6ff;
}

.comment-float__send {
  background: #1f6feb;
  color: #fff;
  border: none;
  border-radius: 5px;
  padding: 5px 12px;
  font-size: 13px;
  cursor: pointer;
}
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npm test src/components/CommentFloat/CommentFloat.test.tsx
```

Expected: 6개 테스트 모두 통과

- [ ] **Step 6: Commit**

```bash
git add src/components/CommentFloat/
git commit -m "feat(frontend): add CommentFloat component with tests"
```

---

## Task 10: MarkdownPane Component

**Files:**
- Create: `src/components/MarkdownPane/index.tsx`
- Create: `src/components/MarkdownPane/MarkdownPane.css`
- Create: `src/components/MarkdownPane/MarkdownPane.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/components/MarkdownPane/MarkdownPane.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { MarkdownPane } from './index'

describe('MarkdownPane', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  it('마크다운을 HTML로 렌더링', () => {
    render(<MarkdownPane content="# 안녕하세요\n\n본문 텍스트" isVisible={true} />)
    expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('안녕하세요')
    expect(screen.getByText('본문 텍스트')).toBeInTheDocument()
  })

  it('isVisible=false 시 숨김 처리', () => {
    const { container } = render(<MarkdownPane content="# 제목" isVisible={false} />)
    const pane = container.querySelector('.markdown-pane')
    expect(pane).toHaveStyle('display: none')
  })

  it('텍스트 선택 시 CommentFloat 표시', async () => {
    const mockRect = {
      top: 100, left: 50, width: 80, height: 20,
      bottom: 120, right: 130, x: 50, y: 100,
      toJSON: () => ({}),
    } as DOMRect

    vi.spyOn(window, 'getSelection').mockReturnValue({
      isCollapsed: false,
      toString: () => '선택된 텍스트',
      getRangeAt: () => ({ getBoundingClientRect: () => mockRect }),
    } as unknown as Selection)

    const { container } = render(
      <MarkdownPane content="# 제목\n\n선택된 텍스트입니다" isVisible={true} />
    )

    fireEvent.mouseUp(container.querySelector('.markdown-pane')!)

    expect(screen.getByText('💬 코멘트')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test src/components/MarkdownPane/MarkdownPane.test.tsx
```

Expected: `Cannot find module './index'`

- [ ] **Step 3: `MarkdownPane/index.tsx` 구현**

```typescript
// src/components/MarkdownPane/index.tsx
import { useMemo } from 'react'
import { marked } from 'marked'
import DOMPurify from 'dompurify'
import { useSelection } from '../../hooks/useSelection'
import { CommentFloat } from '../CommentFloat'
import 'highlight.js/styles/github-dark.css'
import './MarkdownPane.css'

interface Props {
  content: string
  isVisible: boolean
}

export function MarkdownPane({ content, isVisible }: Props) {
  const { selection, handleMouseUp, clearSelection } = useSelection()

  const html = useMemo(() => {
    if (!content) return ''
    const raw = marked.parse(content, { async: false }) as string
    return DOMPurify.sanitize(raw)
  }, [content])

  return (
    <div
      className="markdown-pane"
      style={{ display: isVisible ? 'flex' : 'none' }}
      onMouseUp={handleMouseUp}
    >
      <div
        className="markdown-pane__content"
        dangerouslySetInnerHTML={{ __html: html }}
      />
      {selection.text && selection.rect && (
        <CommentFloat
          selection={{ text: selection.text, rect: selection.rect }}
          onClose={clearSelection}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 4: `MarkdownPane.css` 생성**

```css
/* src/components/MarkdownPane/MarkdownPane.css */
.markdown-pane {
  width: 100%;
  height: 100%;
  background: #0d1117;
  overflow-y: auto;
  flex-direction: column;
  padding: 24px 32px;
  box-sizing: border-box;
  color: #c9d1d9;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 15px;
  line-height: 1.7;
  user-select: text;
}

.markdown-pane__content h1,
.markdown-pane__content h2,
.markdown-pane__content h3 {
  color: #e6edf3;
  border-bottom: 1px solid #21262d;
  padding-bottom: 8px;
  margin-top: 24px;
}

.markdown-pane__content code {
  background: #161b22;
  padding: 2px 6px;
  border-radius: 4px;
  font-family: 'Menlo', 'Monaco', monospace;
  font-size: 13px;
  color: #79c0ff;
}

.markdown-pane__content pre {
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  padding: 16px;
  overflow-x: auto;
}

.markdown-pane__content pre code {
  background: none;
  padding: 0;
  color: #c9d1d9;
}

.markdown-pane__content blockquote {
  border-left: 4px solid #30363d;
  margin-left: 0;
  padding-left: 16px;
  color: #8b949e;
}

.markdown-pane__content a {
  color: #58a6ff;
}

.markdown-pane__content ::selection {
  background: #264f78;
}
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npm test src/components/MarkdownPane/MarkdownPane.test.tsx
```

Expected: 3개 테스트 모두 통과

- [ ] **Step 6: Commit**

```bash
git add src/components/MarkdownPane/
git commit -m "feat(frontend): add MarkdownPane component with tests"
```

---

## Task 11: App Root + Integration

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.css`
- Modify: `index.html`

- [ ] **Step 1: `index.html` — 제목 업데이트**

`<title>` 태그를:
```html
<title>MeeCode</title>
```

- [ ] **Step 2: `App.css` — 앱 전체 스타일**

```css
/* src/App.css */
* { box-sizing: border-box; margin: 0; padding: 0; }

html, body, #root {
  width: 100%;
  height: 100%;
  background: #0d1117;
  overflow: hidden;
}

.app {
  width: 100%;
  height: 100%;
  display: flex;
}

.resize-handle {
  width: 4px;
  background: #21262d;
  cursor: col-resize;
  transition: background 0.15s;
  flex-shrink: 0;
}

.resize-handle:hover,
.resize-handle[data-resize-handle-active] {
  background: #58a6ff;
}
```

- [ ] **Step 3: `App.tsx` 구현**

```typescript
// src/App.tsx
import { useEffect } from 'react'
import { invoke } from '@tauri-apps/api/core'
import { Panel, PanelGroup, PanelResizeHandle } from 'react-resizable-panels'
import { TerminalPane } from './components/TerminalPane'
import { MarkdownPane } from './components/MarkdownPane'
import { usePtyStream } from './hooks/usePtyStream'
import './App.css'

function App() {
  const { markdownContent, isMarkdownVisible } = usePtyStream()

  useEffect(() => {
    invoke('start_session').catch((e) => console.error('Failed to start PTY session:', e))
  }, [])

  return (
    <div className="app">
      <PanelGroup direction="horizontal">
        <Panel defaultSize={isMarkdownVisible ? 50 : 100} minSize={20}>
          <TerminalPane />
        </Panel>
        {isMarkdownVisible && (
          <>
            <PanelResizeHandle className="resize-handle" />
            <Panel defaultSize={50} minSize={20}>
              <MarkdownPane content={markdownContent} isVisible={isMarkdownVisible} />
            </Panel>
          </>
        )}
      </PanelGroup>
    </div>
  )
}

export default App
```

- [ ] **Step 4: `src/main.tsx` 확인**

```typescript
// src/main.tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

- [ ] **Step 5: 전체 테스트 통과 확인**

```bash
npm test
```

Expected: 모든 테스트 통과 (오류 0개)

- [ ] **Step 6: 개발 서버 실행 및 수동 검증**

```bash
npm run tauri dev
```

다음을 순서대로 테스트:
1. 앱 실행 시 터미널에 `claude`가 실행됨
2. 짧은 응답(500자 미만) → 마크다운 패널 열리지 않음
3. 긴 응답(500자 이상) → 오른쪽에 마크다운 패널 자동 렌더링
4. 마크다운에서 텍스트 드래그 선택 → 💬 코멘트 버튼 등장
5. 버튼 클릭 → 버튼 사라지고 플로팅 인풋창 등장
6. `async 없으면?` 입력 후 Enter → 터미널에 `[선택: "..."] async 없으면?` 전송
7. ESC → 인풋창 닫힘
8. 패널 사이 드래그 → 크기 조절 가능

- [ ] **Step 7: 최종 Commit + Push**

```bash
git add src/App.tsx src/App.css src/main.tsx index.html
git commit -m "feat: wire up App root with split layout and session startup"
git push -u origin main
```

---

## 완료 기준

- [ ] `npm test` 전체 통과
- [ ] `npm run tauri dev` 실행 시 오류 없이 앱 시작
- [ ] Claude Code 터미널 정상 작동 (입력/출력)
- [ ] 500자 이상 응답에서 마크다운 패널 자동 활성화
- [ ] 텍스트 선택 → 플로팅 버튼 → 플로팅 인풋 → 터미널 전송 흐름 완료
- [ ] `git push origin main` 완료
